<!--need to style and add review functionality -->
<div id="navigation">
  <%= render "static_pages/header" %>
</div>

<% provide(:title, @user.name) %>
<h1>
<% if @user.profile_img_url == nil %>
  <%= gravatar_for @user %>
  <%= @user.name %>
<%= else %>
  <%#= image_tag []
</h1>

